Usage: nxtpm [--version] [--help]
       nxtpm install <package> [<dir>] [--version=<version>]
       nxtpm init [<dir>]
       nxtpm package [<dir>] [<outfile>] [--format=tgz|zip] [--update [--set-version=<version>] [--set-url=<url>]]
       nxtpm publish [<dir>] [--secret=<passphrase>|--no-sign]
       nxtpm extend <package> [--times=<number>] [--secret=<passphrase>|--no-sign]
       nxtpm info <package>

Options:
 --format=FORMAT         # Package format (tgz or zip)
 --update                # Update nxtpm.json with new sha256 value
 --set-version=VERSION   # Set package version in nxtpm.json
 --set-url=URL           # Set package URL in nxtpm.json
 --times=TIMES           # How many times to extend [default: 1]
 --secret=SECRET         # Nxt secret phrase
 --no-sign               # Do not sign transactions; Output unsigned transaction instead
 --version               # Display nxtpm version
 --help                  # Show this help text
